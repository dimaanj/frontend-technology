const { promiseLimit } = require('./promise-limit');

describe('promiseLimit (template)', () => {
  it('выполняет не более limit задач одновременно', async () => {
    // TODO: напишите тест, который проверит,
    // что одновременно не выполняется больше чем `limit` промисов.
  });

  it('возвращает результаты в исходном порядке', async () => {
    // TODO: напишите тест, который проверит,
    // что результаты идут в том же порядке, что и входной массив tasks,
    // даже если промисы завершаются в разном порядке.
  });

  it('продолжает выполнение при ошибке задачи', async () => {
    // TODO: напишите тест, который проверит,
    // что ошибка одной задачи не мешает выполнению остальных,
    // а в результирующем массиве на позиции упавшей задачи появляется ошибка (или её признак).
  });
});

